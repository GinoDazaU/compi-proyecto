int main() {
    int n = 10000000;
    float sum = 0.0;
    float sign = 1.0;

    int k = 0;
    for (k = 0; k < n; k++) {
        float denom = 2.0 * k + 1.0;
        sum = sum + sign / denom;
        sign = -sign;
    }

    float pi = sum * 4.0;
    println(pi);
    return 0;
}
