int main() {
    int x = 42;
    float y = 3.14e2;
    bool flag = true;
    char c = 'a';
    string msg = "hola mundo";

    // comentario de línea
    /* comentario
       de bloque */

    if (x >= 10 && !flag) {
        x += 1;
    } else {
        x--;
    }

    while (x > 0) {
        x -= 1;
    }

    int* ptr = &x;
    ptr->y;

    return 0;
}
