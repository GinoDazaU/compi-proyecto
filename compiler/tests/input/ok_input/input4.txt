struct Point {
    int x;
    int y;
};

int distSq(Point& p) {
    return p.x * p.x + p.y * p.y;
}

int main() {
    Point p;
    p.x = 3;
    p.y = 4;
    int d = distSq(p);
    println(d);
    return 0;
}
